<!DOCTYPE html>
<html>
  <head>
    <title>TestGuru</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="nav user"><%= link_to "test-guru", root_path %></p>
    <% if logged_in? %>
      <p class="nav user">Welcome, <b><%= current_user.email %></b> Guru</p>
      <p class="nav user"><%= link_to "Logout", logout_path, method: :delete %></p>   
    <% else %>
      <p class="nav user"><%= link_to "signup", signup_path %></p>
      <p class="nav user"><%= link_to "login", login_path %></p>
    <% end %>

    <%= yield %>
  </body>

  <footer> 
    <%= render "shared/footer" %>
  </footer>
</html>
